# 🎨 Color Generator - React Coursework

Интерактивное React приложение для генерации случайных цветов с поддержкой копирования, истории и горячих клавиш.

## ✨ Возможности

- 🎨 **Генерация случайных цветов** — каждый клик создаёт новый цвет
- 📋 **Копирование в буфер** — одна кнопка для копирования HEX-кода
- ⌨️ **Клавиша Space** — быстрая генерация без мыши
- 💾 **История последних 5 цветов** — с сохранением в Local Storage
- 🎯 **Отзывчивый дизайн** — работает на всех устройствах
- 🌈 **RGB отображение** — видишь оба формата цвета

## 📸 Скриншоты

![Color Generator App](https://user-gen-media-assets.s3.amazonaws.com/seedream_images/bcd33802-15c3-4864-8cb0-8f4fc3e27680.png)
*Главный интерфейс приложения с генератором цветов*

## 🛠️ Технологический стек

- **React 18.x** — UI фреймворк
- **React Hooks** (useState, useEffect) — управление состоянием
- **CSS3** — современные стили и анимации
- **Clipboard API** — работа с буфером обмена
- **Local Storage** — сохранение истории

## 📦 Установка и запуск

### Требования
- Node.js (v14 или выше)
- npm или yarn

### Инструкции

1. **Клонирование репозитория:**
```bash
git clone https://github.com/[твой-username]/color-generator.git
cd color-generator
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Запуск в режиме разработки:**
```bash
npm start
```

Приложение откроется на [http://localhost:3000](http://localhost:3000)

4. **Построение для продакшена:**
```bash
npm run build
```

## 🎮 Как использовать

### Основные действия:
1. **Кнопка "Сгенерировать цвет"** — создаёт и отображает случайный цвет
2. **Кнопка "Копировать"** — копирует HEX-код в буфер обмена
3. **Клавиша Space** — альтернатива кнопке генерации
4. **Клик на цвет в истории** — выбирает этот цвет

## 📋 Структура проекта

```
color-generator/
├── public/
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── App.js           # Главный компонент
│   ├── App.css          # Стили приложения
│   ├── index.js         # Точка входа
│   └── index.css        # Глобальные стили
├── package.json         # Зависимости проекта
└── README.md            # Этот файл
```

## 💻 Структура кода

### Основные функции:

**generateColor()** — создание случайного HEX-цвета
```javascript
const generateColor = () => {
  const randomColor = '#' + Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, '0')
    .toUpperCase();
  setCurrentColor(randomColor);
  addToHistory(randomColor);
};
```

**copyToClipboard()** — копирование цвета
```javascript
const copyToClipboard = () => {
  navigator.clipboard.writeText(currentColor).then(() => {
    setCopyMessage('✓ Скопировано!');
    setTimeout(() => setCopyMessage(''), 2000);
  });
};
```

**Поддержка Space** — горячая клавиша
```javascript
useEffect(() => {
  const handleKeyPress = (e) => {
    if (e.code === 'Space') {
      e.preventDefault();
      generateColor();
    }
  };
  window.addEventListener('keydown', handleKeyPress);
  return () => window.removeEventListener('keydown', handleKeyPress);
}, []);
```

## 🎓 Требования к контрольной работе

✅ **Компоненты React** — использование функциональных компонентов  
✅ **State Management** — useState для управления состоянием  
✅ **Event Handling** — обработка событий (click, keydown)  
✅ **Условная визуализация** — отображение истории цветов  
✅ **Стили и CSS** — адаптивный дизайн  
✅ **Архитектура** — чистая структура кода  
✅ **Git & GitHub** — версионирование кода  

## 🚀 Возможные улучшения

- [ ] Добавить RGB и HSL форматы цветов
- [ ] Реализовать избранные цвета (favorites)
- [ ] Экспорт истории в JSON
- [ ] Dark Mode
- [ ] Палитры (монохромная, контрастная схема)
- [ ] Анимация переходов между цветами
- [ ] PWA поддержка

## 📝 Лицензия

MIT License — свободное использование в образовательных целях

## 👤 Автор

**Студент:** [Твоё имя]  
**Дата:** 26 ноября 2025  
**Курс:** Веб-разработка на React  

---

**Создано как контрольная работа для демонстрации навыков React разработки** 🎓